const Errors = {
	EvenNumber: 'You got an even number, try again.',
};

/**
 * @type {RandomConfig}
 */
const DEFAULT_RANDOM_CONFIG = {
	min: 1,
	max: 10,
};

module.exports = { Errors, DEFAULT_RANDOM_CONFIG };
